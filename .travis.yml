language: scala

scala:
  - 2.11.11

script:
  - echo "Testing to be enabled later" #sbt clean coverage test coverageReport &&
    #sbt coverageAggregate
#after_success:
#  - sbt coveralls
#  - bash <(curl -s https://codecov.io/bash)
